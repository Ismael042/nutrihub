fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.0
pydantic-settings==2.3.4
reportlab==4.2.0
python-dotenv==1.0.1
asyncpg==0.29.0
passlib==1.7.4
bcrypt==4.0.1
pyjwt==2.8.0
google-auth==2.34.0
requests==2.32.3   # transporte usado por google.auth.transport.requests (verificação do ID token)
# Usado em runtime por app/core/email.py (Resend) e por pytest/TestClient nos testes.
httpx==0.27.2
# Upload da foto de perfil (POST /me/public-profile/photo). O fastapi puro não puxa
# python-multipart — sem ele, UploadFile/File() estouram RuntimeError já no import.
python-multipart==0.0.9
# R2 é S3-compatível; a Cloudflare não expõe API REST de objeto, só a S3.
# >= 1.36 é obrigatório: as flags request_checksum_calculation/response_checksum_validation
# passadas em app/core/storage.py só existem a partir dessa versão do botocore (em
# versões antigas o Config levanta "unexpected keyword argument").
boto3==1.40.76
# Re-encoda a imagem: normaliza formato/dimensão e, principalmente, descarta o EXIF
# (foto de celular carrega GPS, e essa imagem vai pra uma página pública).
Pillow==10.4.0

# Dev/test only
pytest==8.3.2
pytest-asyncio==0.24.0
